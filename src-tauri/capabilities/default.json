{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Extractr 默认窗口权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:default",
    "opener:allow-open-path",
    "opener:allow-reveal-item-in-dir"
  ]
}
