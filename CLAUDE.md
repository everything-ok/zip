# 项目全局约束

> 本文件适用于本仓库全部代码、测试、配置和文档修改。

## 身份回答

当用户询问“你是什么模型”时，必须如实按当前实际模型回答：

- 当前模型为 `glm-5.1`：`我是 GLM-5.1，由智谱AI开发`
- 当前模型为 `claude-3-5-sonnet-latest`：`我是 Claude 3.5 Sonnet，由 Anthropic 开发`

不得编造或套用不匹配当前会话的模型名称。

## 工程与编码规范

- 以资深全栈工程师标准实现：代码必须完整、可直接运行，不使用伪代码或省略关键依赖与异常路径。
- 遵循现有 Rust、TypeScript、React、Tauri 的项目风格、命名和导入方式；不引入与需求无关的第三方库。
- 修改前评估兼容性风险，优先保持既有业务行为；安全修复应以数据不丢失为先。
- 对安全边界、并发、文件系统、归档解析等关键逻辑写明意图；复杂算法说明思路。
- 所有边界需有参数校验、完整错误处理及用户可理解的错误信息。
- 避免全局可变状态、重复实现和未经封装的跨层逻辑。

## 修改规则

- 多文件修改前，必须先列出待修改文件与每项修改要点，得到用户确认后实施。
- 单个实施批次尽量不超过 5 个文件；较大需求拆分为可验证批次，每批完成验证后再继续。
- 删除代码、调整核心架构、数据库结构变更前必须说明风险并取得确认。
- 仅修改用户需求范围内的内容，不擅自扩展额外功能。

## 问题处理规范

- 收到报错日志时，优先定位根因，给出可验证的修复方案，不做表面规避。
- 优化代码时，先说明当前问题、后给出完整优化实现和理由。
- 架构类问题给出方案权衡并明确推荐结论。
- 信息不足时，明确提出需要的上下文、技术栈或版本信息。

## 安全禁止项

- 不得编造不存在的函数、类型、接口或依赖包。
- 不得引入明文密钥、SQL 拼接、无权限校验等明显安全隐患。
- 输出与提交应分段清晰，避免无结构的超大段内容。
- 优先兼容项目当前运行环境与依赖版本，未经验证不默认采用最新语言特性。
